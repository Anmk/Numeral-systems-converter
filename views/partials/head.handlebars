<!DOCTYPE html>
<html lang="en">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta charset="UTF-8">
  <title>{{ title }}</title>

  {{#if styles }}
    {{#each styles}}
      <link rel="stylesheet" href="/css/{{ this }}">
    {{/each}}
  {{/if}}

  {{#if urls }}
    {{#each urls}}
      <link rel="stylesheet" href="https://{{ this }}">
    {{/each}}
  {{/if}}

</head>
<body>
  